# Example demo

Edit `demo.tape` for terminal actions, `demo.json` for composition and timing, and `captions.json` for captions. Commands run from the repository root in a controlled VHS shell. Keep fixture behavior deterministic and network-free.

Run `pnpm demo:render example-demo --all`, then `pnpm demo:inspect example-demo --format landscape`.
